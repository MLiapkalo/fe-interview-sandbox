import { Element } from "deno-dom";

export function getUniqueTags(element: Element): string[] {
  // TODO: Find all unique tag names in element and descendants, return sorted array
  throw new Error("Not implemented");
}
